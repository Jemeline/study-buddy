import { getWeightedSum } from '../components/Survey/MatchingAlgorithm';
import { apiGetStudentProfiles } from '../utils/api';


describe("Matching Algorithm Tests", () => {
    it("Returns an array of integers", (done) => {
        const user = {
            "id": "TESTID",
            "courseSchedule": []
        }
    })
})